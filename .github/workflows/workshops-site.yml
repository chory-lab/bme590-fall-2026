name: Workshops Site

# Builds the JupyterLite workshop site (the browser-runnable class) and
# publishes it as the bme590-fall-2026 GitHub Pages site. See
# jupyterlite/build.py and jupyterlite/README.md.

on:
  push:
    branches: [main]
    paths:
      - "workshops/**"
      - "data/**"
      - "plr_workshops/**"
      - "jupyterlite/**"
      - "pyproject.toml"
      - ".github/workflows/workshops-site.yml"
  pull_request:
    paths:
      - "workshops/**"
      - "plr_workshops/**"
      - "jupyterlite/**"
      - ".github/workflows/workshops-site.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install the build environment
        # pylabrobot (deck.html), the JupyterLite toolchain, and Pillow (the
        # navbar logo shrink) all run host-side; numpy/pandas/pillow for the
        # *kernel* ship inside Pyodide's own lockfile.
        run: |
          pip install --upgrade pip
          pip install \
            "pylabrobot==0.2.2" \
            pillow \
            "jupyterlite-core==0.8.1" \
            "jupyterlite-pyodide-kernel" \
            jupyter-iframe-commands \
            jupyterlab-widgets ipywidgets \
            jupyter-server

      - name: Build the JupyterLite site
        run: python jupyterlite/build.py --deploy deploy
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Sanity-check the deploy root
        run: |
          test -f deploy/index.html
          test -f deploy/bridge.js
          test -f deploy/deck.html
          test -f deploy/output/lab/index.html
          test -f deploy/output/files/00_plr_introduction.ipynb
          test -f deploy/output/extensions/anywidget/package.json

      - uses: actions/upload-pages-artifact@v3
        with:
          path: deploy

  deploy:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
