# Render environment for the cookbook.
#
# PyLabRobot is pinned: the book documents 0.2.2 specifically, and CI renders
# with an empty _freeze cache so every recipe executes against this version.
# A recipe that breaks against the pin fails the build (execute.error: false).
PyLabRobot==0.2.2

# Quarto's jupyter engine needs a kernel and the notebook plumbing.
jupyter==1.1.1
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.11.0
