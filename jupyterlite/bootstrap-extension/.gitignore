# Generated by `npm run build` (tsc + jupyter labextension build), which
# build.py runs on every site build.
#
# Deliberately not committed. A checked-in bundle is a second source of truth
# for which JupyterLab version the extension shares singletons with, and this
# project has already lost a day to exactly that class of drift: an extension
# built against 4.6.3 against an app serving 4.6.0 does not activate, and says
# so only in a console warning.
lib/
labextension/
node_modules/
tsconfig.tsbuildinfo
*.tgz
