{
  "name": "jupyterlite-driver",
  "private": true,
  "description": "Dev-only deps for drive.cjs (headless Chrome driver) and bridge-entry.mjs (comlink host bridge). Nothing ships to the site.",
  "dependencies": {
    "comlink": "^4.4.1",
    "jupyter-iframe-commands-host": "^0.3.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
